import { AuraLoading } from "@/components/feedback/AuraLoading";

export default function KoszykLoading() {
  return (
    <AuraLoading
      message="Składamy zamówienie…"
      hint="Liczymy ile zostało do darmowej wysyłki."
    />
  );
}
